import React from "react";

export default function Head({params}: { params: { slug: string } }) {
  return (
    <React.Fragment>
      <title>Words | Study english</title>
    </React.Fragment>
  );
};
